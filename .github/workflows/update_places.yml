name: Update places.json

on:
  schedule:
    # Runs every night at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:  # allows manual trigger from GitHub UI

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch places from Notion
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
        run: python fetch_places.py

      - name: Commit updated places.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add places.json
          git diff --staged --quiet || git commit -m "chore: update places.json [$(date -u '+%Y-%m-%d %H:%M UTC')]"
          git push

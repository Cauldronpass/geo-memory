import SwiftUI

struct DiscoverView: View {
    var body: some View {
        NavigationStack {
            Text("Discover coming soon")
                .foregroundStyle(.secondary)
                .navigationTitle("Discover")
        }
    }
}
